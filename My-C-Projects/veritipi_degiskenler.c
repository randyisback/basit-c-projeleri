#include <stdio.h>
int main (){
	/*int sayi=60;
	printf("%d\n",sayi);
	*/
	
	/*char harf='A';
	printf("HARF: %c\nASCII Kodu: %d",harf,harf);
	*/
	
	float kesirli = 3.6;
	double uzunkesirli = 3.644522;
	printf("Float verisi = %.2f\nDouble verisi = %.10lf", kesirli, uzunkesirli);
	
	
	
	/* 
	TEMEL DEÐÝÞKENLER
	int veri tipi 4byte(32 bit) = Tam sayý. örnek: 2,30,100... %d ile yazýlýr.
	char veri tipi 1byte(8 bit) = Harfleri ASCII tablosundan alýr. örnek: a,b,c,A,B,C... %c ile yazýlýr.
	float veri tipi Kesirli sayýlarý tutmak için kullanýlýr. örnek: 3.6 , 5.2 , 1.7... %f ile yazýlýr.
	double veri tipi Kesiirli sayýlarý tutmak için kullanýlýr. ama floattan daha kapsamlýdýr. örnek: 3,645421 %lf ile yazýlýr.	
	*/
	
	
	return 0;
}
